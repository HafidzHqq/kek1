{
  "name": "kek1",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "ajv": "^6.12.6",
    "ajv-keywords": "^3.5.2"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
